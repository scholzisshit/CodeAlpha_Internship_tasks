# requirements for Task 1: Credit Scoring Model
# run: pip install -r requirements.txt
# (if pip doesn't work try pip3. if that doesn't work... good luck)

pandas==2.1.0
numpy==1.24.3
scikit-learn==1.3.0
matplotlib==3.7.2
seaborn==0.12.2
